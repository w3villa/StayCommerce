<div class="card mb-7">
  <div class="card-header">
    <div class="row align-items-center">
      <div class="col-sm-6 col-12 mb-4 mb-sm-0">
          <!-- Title -->
          <h1 class="h3 mb-0 ls-tight">Cities</h1>
      </div>
      <!-- Actions -->
      <div class="col-sm-6 col-12 text-sm-end">
          <div class="mx-n1">
          <%= link_to '#', class:"btn d-inline-flex btn-sm btn-primary mx-1" , id: 'add-new-city' do %>
              <span class=" pe-2">
              <i class="bi bi-plus"></i>
              </span>
              <span>Create New City</span>
          <%end%>
          </div>
      </div>
    </div>  

  <%= form_with url: admin_state_cities_path(@state), method: :get, local: true do %>
    <div class="form-group">
      <%= label_tag :state_id, 'Select Country' %>
      <%= select_tag :state_id, options_from_collection_for_select(Stay::State.all, :id, :name), id: 'state-select', class: 'form-control' %>
    </div>
  <% end %>
  </div>


  <div class="table-responsive" id="citiesContainer">
    <table class="table table-hover table-nowrap">
      <thead class="table-light">
    <tr>
      <th scope="col">Name</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @cities.each do |city| %>
      <tr>
        <td><%= city.name %></td>
        <td class="text-end d-flex justify-content-end">
          <%= link_to edit_admin_state_city_path(@state, city), class: 'btn d-inline-flex btn-sm btn-neutral mx-1' do %>
            <i class="bi bi-pencil">Edit</i>
          <% end %>  
          <%= button_to admin_state_city_path(@state, city), method: :delete, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'btn btn-sm btn-neutral mx-1 text-danger-hover' do %>
            <i class="bi bi-trash">Destroy</i>
          <% end %>                
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

  <div class="card-footer border-0 py-5">
    <span class="text-muted text-sm">Showing 10 items out of 250 results found</span>
  </div>
</div>  



