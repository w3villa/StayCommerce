<div class="container mx-auto p-4">
  <div class="row mt-4 justify-content-center">
    <div class="col-md-8 col-lg-10"> 
      <div class="card">
        <div class="card-header">
          <h2 class="text-xl font-semibold mb-0">Property Type Details</h2> 
        </div>
        <div class="card-body">
          <p><strong>Name: </strong><%= @property_type.name %></p>
          <div class="d-flex mt-4 p-4 justify-content-center">  
            <%= link_to 'Edit', edit_admin_property_type_path(@property_type), class: 'btn btn-warning me-3' %> 
            <%= link_to 'Back', admin_property_types_path, class: 'btn btn-secondary' %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
